apiVersion: batch/v1
kind: Job

metadata:
  name: hello-job
  namespace: project-12

spec:
  template:
    spec:
      restartPolicy: Never

      containers:
        - name: hello
          image: busybox

          command:
            - /bin/sh
            - -c
            - echo "Hello from Kubernetes Job"; sleep 5